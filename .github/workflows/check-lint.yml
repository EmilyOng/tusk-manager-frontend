name: Check lint

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: actionsx/prettier@v2

    with:
      # prettier CLI arguments.
      args: --check .
